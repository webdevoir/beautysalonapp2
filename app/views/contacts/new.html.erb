<%= simple_form_for @contact do |f| %>
  <fieldset>
      <%= f.input :name, label: "Naam" %>
      <%= f.input :email, label: "Email" %>
      <%= f.input :phone, label: "Telefoon" %>
      <%= f.input :message, as: :text, label: "Gewenste behandeling" %>
    <%= f.button :submit, "Maak een Afspraak" %>
  </fieldset>
<% end %>
